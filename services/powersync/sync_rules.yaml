# yaml-language-server: $schema=https://unpkg.com/@powersync/service-sync-rules@0.20.0/schema/sync_rules.json

# Docs https://docs.powersync.com/usage/sync-rules

bucket_definitions:
  global:
    data:
      - SELECT * FROM posts
      - SELECT * FROM comments
